import { expect } from "chai";
import "mocha";
import sinon from "sinon";

describe("Index", () => {
    it("serializes Position protocol buffer to string", () => {
        //Arrange

        //Act

        //Assert
        expect(0).to.equal(1);
    });
});